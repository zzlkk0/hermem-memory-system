[project]
name = "hermes-memory-system"
version = "1.0.0"
description = "Structured long-term memory system for Hermes Agent"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
